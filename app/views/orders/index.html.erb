<p id="notice"><%= notice %></p>

<h1>Welcome to the icecream page!</h1>
<%= image_tag("http://cliparts.co/cliparts/rcL/ngd/rcLngdr7i.png", :class => 'icecream',:alt => "rss feed") %>

<!-- <table>
  <thead>
    <tr>
      <th>Flavour</th>
      <th>Amount</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.flavour %></td>
        <td><%= order.amount %></td>
        <td><%= link_to 'Show', order %></td>
        <td><%= link_to 'Edit', edit_order_path(order) %></td>
        <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path %>
