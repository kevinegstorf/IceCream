
<%= form_for(@order, :as => :order, :url => (@order.new_record? ? order_url : order_url(@order) )) do |f| %>
   <% if @sport_event.errors.any? %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :type %>

  <div class="field">
    <%= f.label :flavour %><br>
    <%= f.select :flavour, @order.class.kinds %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
